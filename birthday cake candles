int birthdayCakeCandles(vector<int> candles) {
    int r,count=0;
  sort(candles.begin(),candles.end());
   r=candles[candles.size()-1];
  for(int i=0;i<candles.size();i++)
  {
      if(candles[i]==r)
      {
          count++;
      }
  }
  return count;

}
