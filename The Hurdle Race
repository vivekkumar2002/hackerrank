int hurdleRace(int k, vector<int> height) {
 int max=0;
    int doses;
    for(int i=0;i<height.size();i++)
    {
        if(height[i]>max)
        max=height[i];
    }
    doses=max-k;
    if(doses>0)
    return doses;
    else
     return 0;

}
